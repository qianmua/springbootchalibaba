server:
  port: 9001
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        # 注册地址
        server-addr: 127.0.0.1:8848
# 暴漏监控
management:
  endpoints:
    web:
      exposure:
        include: '*'
